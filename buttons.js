export default class startButton {
  constructor(x, y, w, h) {
    this.x = x;
    thtis.y = y;
    this.w = w;
    this.y = y;
  }
  draw() {
    fill(250, 250, 250);
    React(this.x, this.y, this.w, this.h);
  }
}
